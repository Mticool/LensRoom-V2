# Быстрая проверка: Seedream 4.5

## ✅ Что исправлено

Seedream 4.5 теперь **правильно передает параметр `quality`** в KIE API.

## 🧪 Как проверить (1 минута)

1. Откройте http://localhost:3000/generator
2. **Settings** → выберите модель **Seedream 4.5**
3. Выберите качество **Turbo**
4. Введите промпт: `"A sunset over mountains"`
5. Откройте **DevTools Console** (F12)
6. Запустите генерацию
7. Проверьте лог в консоли:

### ✅ Должно быть:
```javascript
[API] Generating image with params: {
  model: 'seedream/4.5-text-to-image',
  quality: 'turbo',  // ← ЭТО ВАЖНО!
  aspectRatio: '1:1'
}
```

### ❌ НЕ должно быть:
```javascript
{
  resolution: '1K',  // ← НЕ должно быть resolution!
}
```

## 🎯 Быстрые тесты

```bash
# Запустить юнит-тесты (14 тестов)
node test-photo-params.js

# Ожидаемый результат: ✅ Passed: 14/14
```

## 📋 Проверка других моделей

| Модель | Параметр | Значения |
|--------|----------|----------|
| Seedream 4.5 | `quality` | turbo/balanced/quality |
| Nano Banana Pro | `resolution` | 2K/4K |
| FLUX 2 Pro | `resolution` | 1K/2K |
| Topaz Upscale | `resolution` | 2K/4K/8K |
| Z-image | `quality` | turbo/balanced/quality |
| GPT Image 1.5 | `quality` | medium/high |

## ✅ Готово к деплою

После успешной проверки можно деплоить на production.
