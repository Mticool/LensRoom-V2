# 🚨 Сайт не открывается - Быстрая проверка

## ⚡ Самые частые причины и решения

### 1. PM2 процесс упал (90% случаев)
```bash
ssh root@104.222.177.29
cd /opt/lensroom/lensroom-v2
pm2 restart lensroom
pm2 logs lensroom --lines 20
```

### 2. Нет build (после изменений кода)
```bash
cd /opt/lensroom/lensroom-v2
npm run build
pm2 restart lensroom
```

### 3. Ошибки в коде
```bash
cd /opt/lensroom/lensroom-v2
npm run type-check
npm run build
# Если есть ошибки - исправьте их
pm2 restart lensroom
```

### 4. Порт занят
```bash
lsof -i :3002
kill -9 <PID>
pm2 restart lensroom
```

## 📋 Чеклист (выполните по порядку)

```bash
# 1. Проверка PM2
pm2 status

# 2. Если stopped - перезапуск
pm2 restart lensroom

# 3. Проверка логов
pm2 logs lensroom --lines 50

# 4. Проверка build
cd /opt/lensroom/lensroom-v2
ls -la .next

# 5. Если нет .next - пересборка
npm run build

# 6. Проверка порта
curl http://localhost:3002
```

## 🔍 Что искать в логах

```bash
pm2 logs lensroom --lines 100 | grep -i "error\|failed\|cannot"
```

**Типичные ошибки:**
- `Cannot find module` → проблема с импортами
- `SyntaxError` → синтаксическая ошибка в коде
- `Port 3002 already in use` → порт занят
- `Build failed` → ошибка сборки

## ✅ Последние изменения (проверены)

- ✅ ImageGalleryMasonry - структура исправлена
- ✅ StudioWorkspaces - layout исправлен
- ✅ GeneratorBottomSheet - проверка canGenerate добавлена
- ✅ create/studio/page.tsx - Suspense добавлен

Все изменения проверены на синтаксические ошибки.

---

**Если проблема сохраняется, пришлите:**
```bash
pm2 logs lensroom --lines 200
```
