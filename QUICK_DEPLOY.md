# 🚀 Быстрый деплой генераторов фото

## ✅ Проверка перед деплоем (5 минут)

```bash
cd lensroom-v2

# 1. Автотесты
node test-all-generators.js

# 2. TypeScript
npm run type-check

# 3. Build
npm run build
```

Если все 3 команды успешны → можно деплоить!

---

## 📦 Деплой на сервер

### Вариант 1: Через Git (рекомендуется)

```bash
# На сервере
ssh root@104.222.177.29
cd /opt/lensroom/lensroom-v2
git pull origin main
npm ci
npm run build
pm2 restart lensroom
pm2 logs lensroom --lines 20
```

### Вариант 2: Ручной деплой

```bash
# 1. Скопируйте файлы генераторов на сервер
# 2. На сервере:
cd /opt/lensroom/lensroom-v2
npm ci
npm run build
pm2 restart lensroom
```

---

## 🧪 Проверка после деплоя

1. Откройте: `https://lensroom.ru/create`
2. Проверьте ModelSelector (верхняя панель)
3. Переключите между моделями
4. Попробуйте сгенерировать тестовое изображение

---

## ❗ Если что-то не работает

### Build не проходит?
```bash
rm -rf .next node_modules/.cache
npm run build
```

### Генератор не отображается?
- Проверьте консоль браузера (F12)
- Проверьте логи: `pm2 logs lensroom`
- Убедитесь, что модель имеет `featured: true` в `models.ts`

### API ошибки?
- Проверьте `KIE_API_KEY` в `.env.local`
- Проверьте логи API: `pm2 logs lensroom --lines 100`

---

## 📋 Полная инструкция

См. `PHOTO_GENERATOR_DEPLOY_CHECKLIST.md` для детального чеклиста.

---

**Готово! 🎉**
