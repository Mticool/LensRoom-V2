# Higgsfield-Style Video Generator UI

Новый интерфейс видео-генератора, вдохновленный дизайном Higgsfield.ai.

## 🎨 Особенности дизайна

### Цветовая схема
- **Background**: `#0A0A0A` (глубокий черный)
- **Cards**: `#161616` (темно-серый)
- **Borders**: `#262626` (светло-серый)
- **Accent**: `#D4FF00` (лаймовый зеленый)

### Компоненты

#### 1. **Tabs Switcher**
Горизонтальное меню с тремя вкладками:
- Create Video - базовая генерация
- Edit Video - редактирование существующих видео
- Motion Control - продвинутое управление движением

#### 2. **Model Card**
- Градиентный фон (blue → purple)
- Отображение текущей модели
- Кнопка "Change" для выбора другой модели

#### 3. **Upload Areas**
- Пунктирные границы
- Drag & drop поддержка
- Preview загруженных файлов

#### 4. **Prompt Section**
- Textarea с прозрачным фоном
- Кнопка "Enhance" с иконкой искр
- Сохранение состояния включения/выключения

#### 5. **Settings**
- Блоки с темным фоном
- Стрелки навигации справа
- Hover эффекты

#### 6. **Generate Button**
- Цвет: `#D4FF00`
- Черный текст
- Отображение стоимости генерации
- Анимация загрузки

## 📁 Структура файлов

```
src/
├── components/
│   └── video/
│       └── VideoGeneratorHiggsfield.tsx  # Основной компонент
└── app/
    └── video/
        └── higgsfield/
            └── page.tsx                   # Страница демонстрации
```

## 🚀 Использование

### Базовый пример

```tsx
import { VideoGeneratorHiggsfield } from '@/components/video/VideoGeneratorHiggsfield';

function MyPage() {
  const handleGenerate = (params) => {
    console.log('Generation params:', params);
  };

  return <VideoGeneratorHiggsfield onGenerate={handleGenerate} />;
}
```

### URL для просмотра

Откройте в браузере: `http://localhost:3000/video/higgsfield`

## 🎯 Функциональность

### Create Video Tab
- Загрузка референсного изображения
- Ввод текстового промпта
- Улучшение промпта (Enhance)
- Настройка аудио
- Выбор модели, длительности, aspect ratio

### Motion Control Tab
- Загрузка видео с движением
- Загрузка изображения персонажа
- Выбор качества (720p/1080p)
- Scene control mode (Video/Image)
- Продвинутые настройки

### Edit Video Tab
- Загрузка видео для редактирования (3-10 сек)
- Загрузка дополнительных элементов (до 4)
- Текстовые инструкции для изменений
- Auto settings toggle

## 🔧 Технологии

- **React** - компонентная архитектура
- **TypeScript** - типизация
- **Tailwind CSS** - стилизация
- **Lucide React** - иконки
- **Sonner** - toast уведомления

## 📝 Состояние компонента

```tsx
{
  activeTab: 'create' | 'edit' | 'motion',
  selectedModel: string,
  prompt: string,
  enhanceOn: boolean,
  audioEnabled: boolean,
  duration: string,
  aspectRatio: string,
  sceneControlMode: 'video' | 'image',
  isGenerating: boolean,
  // ... файлы и превью
}
```

## 🎨 Кастомизация

### Изменение accent цвета

Замените `#D4FF00` на свой цвет в файле `VideoGeneratorHiggsfield.tsx`:

```tsx
// Найдите все вхождения bg-[#D4FF00] и замените на свой цвет
className="bg-[#YOUR_COLOR]"
```

### Добавление новой вкладки

1. Добавьте новый тип в `Tab`:
```tsx
type Tab = 'create' | 'edit' | 'motion' | 'your-new-tab';
```

2. Добавьте кнопку в Tabs Switcher
3. Добавьте условный рендеринг контента

## 🐛 Известные ограничения

- Загрузка файлов использует data URLs (не для production)
- Нет реальной интеграции с API (mock)
- Preview area показывает только placeholder

## 🔄 Следующие шаги

- [ ] Интеграция с реальным API генерации
- [ ] Загрузка файлов в S3/CDN
- [ ] Polling статуса генерации
- [ ] История генераций
- [ ] Сохранение пресетов
- [ ] Галерея примеров

## 📞 Контакты

При возникновении вопросов или багов создайте issue в репозитории.
