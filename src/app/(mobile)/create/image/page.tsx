import { ImageGenerator } from '@/components/mobile/ImageGenerator';

export const metadata = {
  title: 'Создать фото | LensRoom AI',
  description: 'Генерация изображений с помощью AI моделей',
};

export default function ImageGeneratorPage() {
  return <ImageGenerator />;
}
