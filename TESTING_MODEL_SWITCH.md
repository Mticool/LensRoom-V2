# Тестирование: Скрытие истории при выборе нейросети

## 🎯 Что исправлено

История теперь **автоматически скрывается** когда пользователь переключается на новую нейросеть или режим, показывая что начинается новая сессия работы.

---

## ✅ Тест 1: Переключение режима (Фото ↔ Видео)

### Шаги:
1. Откройте https://lensroom.ru/generator
2. Создайте любое фото
3. История должна открыться автоматически (на desktop)
4. Кликните на переключатель **"Видео"** в верхней панели

### Ожидаемый результат:
- ✅ История **автоматически скрылась**
- ✅ Индикатор изменился на "Новый чат"
- ✅ Модель автоматически сменилась на видео-модель (например, veo-3.1)

### Повторите в обратную сторону:
5. Кликните на **"Фото"**

### Ожидаемый результат:
- ✅ История снова скрылась
- ✅ Индикатор "Новый чат" виден
- ✅ Модель автоматически вернулась на фото-модель

---

## ✅ Тест 2: Выбор новой модели в настройках

### Шаги:
1. Откройте генератор в режиме **Фото**
2. Создайте фото (чтобы история открылась)
3. Откройте **Settings** (панель слева на desktop, или Menu на мобильном)
4. В разделе "Модель" выберите другую модель (например, `flux-2-pro` вместо `nano-banana`)

### Ожидаемый результат:
- ✅ История **автоматически скрылась**
- ✅ Индикатор изменился на "Новый чат"
- ✅ Модель сменилась на выбранную

### Повторите с другими моделями:
5. Выберите `grok-imagine`
6. Выберите `seedream-4.5`

### Ожидаемый результат каждый раз:
- ✅ История скрывается
- ✅ "Новый чат" виден

---

## ✅ Тест 3: Выбор видео-модели

### Шаги:
1. Переключитесь на режим **Видео**
2. Создайте видео
3. Откройте Settings
4. Выберите другую видео-модель (например, `kling-1.6` вместо `veo-3.1`)

### Ожидаемый результат:
- ✅ История скрылась
- ✅ "Новый чат" показывается

---

## ✅ Тест 4: Комбинированный сценарий

### Шаги:
1. Создайте фото с моделью `nano-banana`
2. История открыта, виден "Продолжение чата"
3. Смените модель на `flux-2-pro`
4. ✅ История скрылась, "Новый чат"
5. Создайте фото
6. ✅ История открылась, "Продолжение чата"
7. Переключитесь на режим **Видео**
8. ✅ История скрылась, "Новый чат"
9. Откройте историю вручную (кнопка справа)
10. ✅ История открылась
11. Выберите любое старое фото из истории
12. ✅ История осталась открытой, показывает "Продолжение чата"
13. Кликните **"Новый"**
14. ✅ История скрылась, "Новый чат"

---

## ✅ Тест 5: Первое открытие генератора

### Шаги:
1. Откройте https://lensroom.ru/generator в **новой вкладке** (или очистите localStorage)
2. Подождите загрузки

### Ожидаемый результат:
- ✅ История **скрыта** по умолчанию
- ✅ Индикатор "Новый чат" виден
- ✅ Модель по умолчанию загружена (nano-banana-pro для фото)

---

## ✅ Тест 6: Мобильная версия

### Шаги:
1. Откройте генератор на **мобильном** (или уменьшите ширину браузера < 768px)
2. Создайте фото
3. Смените модель через Menu → Settings

### Ожидаемый результат:
- ✅ История не открывается автоматически на мобильном
- ✅ При смене модели всё равно срабатывает логика скрытия истории (для консистентности)

---

## 🐛 Возможные баги

### Баг #1: История не скрывается при смене модели
**Симптомы:** Выбрали новую модель, но история осталась открытой
**Проверить:**
- Консоль браузера (F12) на ошибки
- Код `GeneratorV2.tsx:196-202` (useEffect для settings.model)

### Баг #2: История скрывается слишком часто
**Симптомы:** История скрывается даже когда меняются другие настройки (качество, размер)
**Проверить:**
- Должна скрываться только при смене `settings.model`, а не всего объекта `settings`
- Код `GeneratorV2.tsx:196` - зависимость должна быть `[settings.model]`

### Баг #3: История скрывается на первой загрузке
**Симптомы:** При первом открытии генератора происходит лишнее срабатывание useEffect
**Проверить:**
- Код `GeneratorV2.tsx:198-201` - проверка `isInitialMount`
- localStorage должен быть пустой при первом открытии

---

## 📝 Технические детали

### Код изменений:

#### 1. Скрытие при смене режима (Фото/Видео)
```typescript
// GeneratorV2.tsx:178-194
useEffect(() => {
  if (mode === 'video' && ...) {
    setSettings(prev => ({...}));
    setShowHistory(false); // <-- Добавлено
  } else if (mode === 'image' && ...) {
    setSettings(prev => ({...}));
    setShowHistory(false); // <-- Добавлено
  }
}, [mode]);
```

#### 2. Скрытие при смене модели
```typescript
// GeneratorV2.tsx:196-202
useEffect(() => {
  const isInitialMount = !localStorage.getItem('lensroom-generator-v2-settings');
  if (!isInitialMount) {
    setShowHistory(false);
  }
}, [settings.model]); // <-- Зависимость только от model
```

---

## ✅ Критерии успешного теста

Тест пройден, если:
1. ✅ История скрывается при переключении Фото ↔ Видео
2. ✅ История скрывается при выборе новой модели
3. ✅ История НЕ скрывается при изменении других настроек (качество, размер)
4. ✅ История скрыта по умолчанию при первом открытии
5. ✅ Кнопка "Новый" работает корректно
6. ✅ Индикаторы "Новый чат" / "Продолжение чата" меняются правильно
7. ✅ Мобильная версия работает без ошибок

---

## 🚀 После тестирования

Если всё работает:
- ✅ Готово к продакшену
- ✅ Можно деплоить

Если есть баги:
- 📝 Опишите баг подробно
- 📸 Приложите скриншот/видео
- 🔍 Проверьте консоль на ошибки
