# ✅ Чеклист проверки и деплоя генераторов фото

**Дата**: 26 января 2026  
**Статус**: Готово к проверке и деплою

---

## 🔍 Шаг 1: Локальная проверка

### 1.1 Проверка файлов генераторов

Все 9 генераторов должны быть в папке `src/components/generator-v2/`:

```bash
cd lensroom-v2
ls -la src/components/generator-v2/*Generator.tsx
```

**Ожидаемые файлы:**
- ✅ `NanoBananaProGenerator.tsx`
- ✅ `NanoBananaGenerator.tsx`
- ✅ `ZImageGenerator.tsx`
- ✅ `FluxProGenerator.tsx`
- ✅ `SeedreamGenerator.tsx`
- ✅ `GPTImageGenerator.tsx`
- ✅ `GrokImagineGenerator.tsx`
- ✅ `TopazUpscaleGenerator.tsx`
- ✅ `RecraftRemoveBGGenerator.tsx`

### 1.2 Запуск автоматических тестов

```bash
cd lensroom-v2
node test-all-generators.js
```

**Ожидаемый результат:**
- Всего тестов: ~93
- Пройдено: ≥91 (98%+)
- Провалено: ≤2 (минорные в Grok Imagine допустимы)

### 1.3 Проверка TypeScript

```bash
cd lensroom-v2
npm run type-check
```

**Ожидаемый результат:**
- ✅ 0 ошибок TypeScript

### 1.4 Проверка сборки

```bash
cd lensroom-v2
npm run build
```

**Ожидаемый результат:**
- ✅ Сборка успешна
- ✅ Нет ошибок компиляции
- ✅ `.next` папка создана

---

## 🧪 Шаг 2: Функциональное тестирование

### 2.1 Запуск dev сервера

```bash
cd lensroom-v2
npm run dev
```

Откройте в браузере: `http://localhost:3000/create`

### 2.2 Проверка UI компонентов

Для каждого генератора проверьте:

1. **ModelSelector** (верхняя панель):
   - ✅ Отображается dropdown с 9 моделями
   - ✅ При выборе модели UI переключается
   - ✅ Показываются правильные цены

2. **Каждый генератор**:
   - ✅ Поле промпта отображается
   - ✅ Quality/Mode селектор работает
   - ✅ Aspect ratio селектор работает
   - ✅ Quantity counter работает (1-4)
   - ✅ Кнопка Generate показывает правильную цену
   - ✅ I2I загрузка работает (где поддерживается)

3. **Специальные генераторы**:
   - **Grok Imagine**: Mode selector вместо Quality
   - **Topaz Upscale**: Обязательная загрузка, нет промпта
   - **Recraft Remove BG**: Обязательная загрузка, нет промпта

### 2.3 Проверка API запросов

Откройте DevTools → Network и попробуйте сгенерировать изображение:

1. **Проверьте запрос к API**:
   - URL: `/api/generate/photo`
   - Method: POST
   - Status: 200 или 202 (для асинхронных)

2. **Проверьте payload**:
   ```json
   {
     "model": "nano-banana-pro",
     "prompt": "test",
     "quality": "1k_2k",
     "aspectRatio": "1:1",
     "variants": 1
   }
   ```

3. **Проверьте ответ**:
   - Успешный ответ содержит `jobId` или `results`
   - Ошибки обрабатываются корректно

---

## 🚀 Шаг 3: Подготовка к деплою

### 3.1 Проверка переменных окружения

Убедитесь, что на сервере настроены все необходимые переменные:

```bash
# На сервере
cd /opt/lensroom/lensroom-v2
cat .env.local | grep -E "KIE_API_KEY|SUPABASE|TELEGRAM"
```

**Обязательные переменные:**
- ✅ `KIE_API_KEY` - ключ KIE.ai API
- ✅ `NEXT_PUBLIC_SUPABASE_URL` - URL Supabase проекта
- ✅ `NEXT_PUBLIC_SUPABASE_ANON_KEY` - Supabase anon key
- ✅ `SUPABASE_SERVICE_ROLE_KEY` - Supabase service role key
- ✅ `TELEGRAM_BOT_TOKEN` - токен Telegram бота

### 3.2 Проверка конфигурации моделей

Проверьте, что все модели правильно настроены в `src/config/models.ts`:

```bash
cd lensroom-v2
grep -E "id: '(nano-banana|z-image|flux-2-pro|seedream|gpt-image|grok-imagine|topaz|recraft)" src/config/models.ts
```

**Ожидаемый результат:**
- Все 9 моделей присутствуют
- Все имеют `featured: true`
- Правильные `apiId` и `provider`

---

## 📦 Шаг 4: Деплой на production

### 4.1 На сервере: обновление кода

```bash
# SSH на сервер
ssh root@104.222.177.29

# Переход в директорию проекта
cd /opt/lensroom/lensroom-v2

# Обновление кода (если используется git)
git pull origin main

# Или копирование файлов вручную
```

### 4.2 Установка зависимостей

```bash
cd /opt/lensroom/lensroom-v2
npm ci
```

### 4.3 Сборка проекта

```bash
cd /opt/lensroom/lensroom-v2
npm run build
```

**Проверьте:**
- ✅ Сборка завершилась успешно
- ✅ Нет ошибок в консоли
- ✅ Папка `.next` создана

### 4.4 Перезапуск PM2

```bash
# Проверка статуса
pm2 status

# Перезапуск приложения
pm2 restart lensroom

# Проверка логов
pm2 logs lensroom --lines 50
```

**Ожидаемый результат:**
- ✅ Приложение запущено
- ✅ Нет ошибок в логах
- ✅ Порт 3002 слушается

### 4.5 Проверка Nginx

```bash
# Проверка конфигурации Nginx
nginx -t

# Перезагрузка Nginx (если нужно)
systemctl reload nginx
```

---

## ✅ Шаг 5: Проверка после деплоя

### 5.1 Проверка сайта

Откройте в браузере: `https://lensroom.ru/create`

**Проверьте:**
1. ✅ Страница загружается
2. ✅ ModelSelector отображается
3. ✅ Все 9 моделей в списке
4. ✅ Переключение между моделями работает
5. ✅ UI каждого генератора отображается корректно

### 5.2 Тестовая генерация

Попробуйте сгенерировать изображение:

1. Выберите модель (например, Nano Banana Pro)
2. Введите промпт: "test image"
3. Нажмите Generate
4. Проверьте:
   - ✅ Запрос отправлен
   - ✅ Показывается прогресс
   - ✅ Результат отображается в галерее

### 5.3 Проверка логов

```bash
# На сервере
pm2 logs lensroom --lines 100
```

**Проверьте:**
- ✅ Нет критических ошибок
- ✅ API запросы обрабатываются
- ✅ Генерации успешны

---

## 🐛 Возможные проблемы и решения

### Проблема 1: Build не проходит

**Симптомы:**
- Ошибки TypeScript
- Ошибки компиляции

**Решение:**
```bash
# Очистка кэша
cd lensroom-v2
rm -rf .next node_modules/.cache
npm run build
```

### Проблема 2: Генератор не отображается

**Симптомы:**
- ModelSelector пустой
- Ошибка в консоли браузера

**Решение:**
1. Проверьте, что модель имеет `featured: true` в `models.ts`
2. Проверьте импорты в `create/page.tsx`
3. Проверьте консоль браузера на ошибки

### Проблема 3: API возвращает ошибку

**Симптомы:**
- 400/500 ошибки при генерации
- "Invalid model" ошибка

**Решение:**
1. Проверьте `apiId` модели в `models.ts`
2. Проверьте `KIE_API_KEY` в `.env.local`
3. Проверьте логи API: `pm2 logs lensroom`

### Проблема 4: Цены неверные

**Симптомы:**
- Неправильная цена на кнопке Generate
- Неверный расчёт стоимости

**Решение:**
1. Проверьте `pricing` в `models.ts`
2. Проверьте `COST_PER_IMAGE` в генераторе
3. Проверьте функцию `useCostCalculation`

---

## 📊 Чеклист готовности к деплою

Перед деплоем убедитесь:

- [ ] Все 9 генераторов на месте
- [ ] Автотесты пройдены (≥91/93)
- [ ] TypeScript проверка: 0 ошибок
- [ ] Build успешен
- [ ] Локальное тестирование пройдено
- [ ] Переменные окружения настроены
- [ ] Конфигурация моделей проверена
- [ ] Документация актуальна

---

## 🎯 Быстрая команда для проверки

```bash
cd lensroom-v2 && \
  echo "1. Тесты..." && node test-all-generators.js && \
  echo "2. TypeScript..." && npm run type-check && \
  echo "3. Build..." && npm run build && \
  echo "✅ Все проверки пройдены!"
```

---

## 📞 Поддержка

Если возникли проблемы:

1. Проверьте логи: `pm2 logs lensroom`
2. Проверьте консоль браузера (F12)
3. Проверьте Network tab в DevTools
4. См. документацию: `ГЕНЕРАТОРЫ_ФИНАЛ.md`

---

**Готово к деплою! 🚀**
