# 📊 Batch Progress Bar - Техническая документация

## Обзор

`BatchProgressBar` - это визуальный компонент для отображения прогресса пакетной обработки изображений в реальном времени. Компонент показывает детальную статистику, временные метрики и позволяет отменить обработку.

## 🎯 Основные возможности

### ✅ Реализованные функции:

1. **Визуальный прогресс-бар**
   - Градиентная полоса прогресса
   - Процентное отображение
   - Счётчик обработанных изображений

2. **Детальная статистика**
   - Готовые изображения (зелёный)
   - Ошибки (красный)
   - Осталось (серый)

3. **Временные метрики**
   - Прошедшее время (00:00)
   - Расчётное время завершения (ETA)
   - Автоматический таймер

4. **Текущий статус**
   - Отображение текущего промпта
   - Анимированный индикатор загрузки
   - Статус обработки

5. **Управление**
   - Кнопка отмены
   - Автоматическое скрытие после завершения
   - Плавные анимации

## 📋 API компонента

### Props

```typescript
interface BatchProgressBarProps {
  progress: BatchProgress;
  className?: string;
  onCancel?: () => void;
}

interface BatchProgress {
  total: number;         // Общее количество изображений
  completed: number;     // Обработано успешно
  failed: number;        // Ошибки
  current: number;       // Текущий индекс (1-based)
  status: 'idle' | 'processing' | 'completed' | 'error';
  currentPrompt?: string; // Текущий промпт
}
```

### Пример использования

```tsx
import { BatchProgressBar, type BatchProgress } from './BatchProgressBar';

function MyComponent() {
  const [progress, setProgress] = useState<BatchProgress>({
    total: 10,
    completed: 0,
    failed: 0,
    current: 0,
    status: 'idle',
  });

  const handleBatchProcess = async () => {
    // Начать обработку
    setProgress(prev => ({ ...prev, status: 'processing' }));

    for (let i = 0; i < 10; i++) {
      // Обновить текущий индекс
      setProgress(prev => ({ ...prev, current: i + 1 }));

      try {
        await processImage(i);
        setProgress(prev => ({ ...prev, completed: prev.completed + 1 }));
      } catch (error) {
        setProgress(prev => ({ ...prev, failed: prev.failed + 1 }));
      }
    }

    // Завершить
    setProgress(prev => ({ ...prev, status: 'completed' }));

    // Сбросить через 3 секунды
    setTimeout(() => {
      setProgress({ total: 0, completed: 0, failed: 0, current: 0, status: 'idle' });
    }, 3000);
  };

  return (
    <>
      <button onClick={handleBatchProcess}>Start Batch</button>
      <BatchProgressBar 
        progress={progress}
        onCancel={() => setProgress({ ...initial, status: 'idle' })}
      />
    </>
  );
}
```

## 🎨 UI/UX особенности

### Позиционирование
```css
position: fixed;
bottom: 24px;
right: 24px;
z-index: 50;
```

### Анимации
- **Появление:** `slide-in-from-bottom-4` (300ms)
- **Прогресс-бар:** `transition-all duration-500`
- **Спиннер:** `animate-spin` (Loader2 icon)

### Цветовая схема
- **Фон:** `#18181B` (тёмно-серый)
- **Граница:** `#27272A`
- **Прогресс:** Градиент `#00D9FF → #7B61FF`
- **Ошибка:** `#EF4444` (красный)
- **Успех:** `#10B981` (зелёный)

### Адаптивность
- Фиксированная ширина: `384px` (24rem)
- Автоматическое позиционирование справа снизу
- Не мешает работе с основным интерфейсом

## 🔄 Интеграция с GeneratorV2

### 1. Импорт компонента

```tsx
import { BatchProgressBar, type BatchProgress } from './BatchProgressBar';
```

### 2. Добавление состояния

```tsx
const [batchProgress, setBatchProgress] = useState<BatchProgress>({
  total: 0,
  completed: 0,
  failed: 0,
  current: 0,
  status: 'idle',
});
```

### 3. Обновление в цикле обработки

```tsx
// Инициализация
setBatchProgress({
  total: batchImages.length,
  completed: 0,
  failed: 0,
  current: 0,
  status: 'processing',
  currentPrompt: prompt,
});

// В цикле
for (let i = 0; i < batchImages.length; i++) {
  setBatchProgress(prev => ({ ...prev, current: i + 1 }));
  
  try {
    await generate(...);
    setBatchProgress(prev => ({ ...prev, completed: prev.completed + 1 }));
  } catch (error) {
    setBatchProgress(prev => ({ ...prev, failed: prev.failed + 1 }));
  }
}

// Завершение
setBatchProgress(prev => ({ ...prev, status: 'completed' }));

// Сброс через 3 секунды
setTimeout(() => setBatchProgress({ ...initialState }), 3000);
```

### 4. Рендеринг компонента

```tsx
<BatchProgressBar
  progress={batchProgress}
  onCancel={() => {
    setBatchProgress({ ...initialState, status: 'idle' });
    toast.info('Обработка отменена');
  }}
/>
```

## ⏱️ Временные метрики

### Elapsed Time (Прошедшее время)
```typescript
const [elapsedTime, setElapsedTime] = useState(0);

useEffect(() => {
  if (!isProcessing) return;
  
  const startTime = Date.now();
  const interval = setInterval(() => {
    setElapsedTime(Math.floor((Date.now() - startTime) / 1000));
  }, 1000);
  
  return () => clearInterval(interval);
}, [isProcessing]);
```

### ETA (Расчётное время завершения)
```typescript
// Формула: (среднее время на 1 изображение) * (осталось изображений)
const eta = (elapsedTime / completed) * (total - completed);
```

## 🎯 Состояния компонента

### 1. `idle` - Не активен
- Компонент скрыт
- Не рендерится

### 2. `processing` - Обработка
- Отображается прогресс-бар
- Спиннер в заголовке
- Таймер активен
- Показывается текущий промпт
- Кнопка "Отмена" доступна

### 3. `completed` - Завершено
- Зелёная галочка в заголовке
- Прогресс-бар заполнен на 100%
- Таймер остановлен
- Автоскрытие через 3 секунды

### 4. `error` - Ошибка
- Красный крестик в заголовке
- Прогресс-бар красного цвета
- Показывается количество ошибок

## 🎨 Кастомизация

### Изменение цветов прогресса

```tsx
// В BatchProgressBar.tsx, строка ~95
<div className={cn(
  "h-full transition-all duration-500 rounded-full",
  progress.status === 'error' 
    ? 'bg-red-500'                           // Цвет ошибки
    : 'bg-gradient-to-r from-cyan-400 to-purple-500' // Ваш градиент
)}
```

### Изменение позиции

```tsx
// Изменить fixed позицию в className:
className="fixed bottom-6 left-6 ..."  // Слева снизу
className="fixed top-6 right-6 ..."    // Справа сверху
```

### Изменение ширины

```tsx
// Изменить w-96 на нужную ширину:
className="... w-80 ..."  // 320px
className="... w-full max-w-md ..."  // Адаптивная
```

## 📊 Статистика и метрики

### Отображаемые метрики:
- ✅ **Готово:** Успешно обработанные изображения
- ❌ **Ошибки:** Изображения с ошибками
- ⏳ **Осталось:** Необработанные изображения
- 📊 **Процент:** % завершения
- ⏱️ **Время:** MM:SS формат
- 🎯 **ETA:** Расчётное время окончания

## 🐛 Обработка ошибок

### Отмена обработки
```tsx
onCancel={() => {
  // Остановить текущую обработку
  abortController.abort();
  
  // Сбросить состояние
  setBatchProgress({ total: 0, completed: 0, failed: 0, current: 0, status: 'idle' });
  
  // Уведомить пользователя
  toast.info('Обработка отменена');
}}
```

### Частичные ошибки
```tsx
// В цикле обработки
try {
  await generate(...);
  successCount++;
} catch (error) {
  failCount++;
  // Продолжаем обработку остальных
}

// В конце показываем статус
if (failCount > 0) {
  setBatchProgress(prev => ({ ...prev, status: 'error' }));
}
```

## ✨ Будущие улучшения

### Планируемые функции:
- [ ] Пауза/Возобновление обработки
- [ ] История обработок
- [ ] Экспорт результатов
- [ ] Звуковое уведомление при завершении
- [ ] Desktop notifications (Web Notifications API)
- [ ] Сохранение прогресса в localStorage
- [ ] Индивидуальный статус каждого изображения
- [ ] Thumbnail превью результатов

## 🔗 Связанные компоненты

- **BatchImageUploader** - Загрузка изображений для batch
- **HistoryImagePicker** - Выбор из истории
- **GeneratorV2** - Главный компонент генератора
- **Toast (sonner)** - Дополнительные уведомления

## 📝 Changelog

### v1.0.0 (Current)
- ✅ Базовая функциональность
- ✅ Визуальный прогресс-бар
- ✅ Временные метрики
- ✅ Статистика обработки
- ✅ Отмена обработки
- ✅ Автоматическое скрытие

---

**Разработано для LensRoom V2 - AI Image & Video Generation Platform** 🚀


