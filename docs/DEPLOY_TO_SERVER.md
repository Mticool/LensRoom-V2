# 🚀 Деплой LensRoom V2 на сервер

## 📋 Текущая ситуация:

**Commit готов:** `af6d64c` - Add Remix & Batch modes  
**Статус:** 41 файл изменён, 6652 строки добавлено  
**Сервер:** PM2 на `/opt/lensroom/current`

---

## 🔧 ВАРИАНТ 1: Деплой через Git (Рекомендуется)

### **Шаг 1: Добавить сервер как Git remote**

На **локальной машине:**

```bash
cd /Users/maratsagimov/Desktop/LensRoom.V2/lensroom-v2

# Добавить сервер как remote (если ещё не добавлен)
git remote add production root@your-server-ip:/opt/lensroom/repo.git

# Или если уже есть GitHub и нужен дополнительный remote:
git remote add server root@your-server-ip:/opt/lensroom/repo.git

# Проверить remotes
git remote -v
```

### **Шаг 2: Push на сервер**

```bash
# Push коммита на сервер
git push production main

# Или если remote назван "server"
git push server main
```

### **Шаг 3: На сервере - применить изменения**

Подключитесь к серверу через SSH:

```bash
ssh root@your-server-ip
```

Затем на сервере:

```bash
# Перейти в папку проекта
cd /opt/lensroom/current

# Pull последних изменений
git pull origin main

# Установить зависимости (если нужно)
npm ci

# Запустить деплой скрипт
bash scripts/deploy-production.sh
```

**Скрипт автоматически:**
- ✅ Установит зависимости
- ✅ Соберёт production build
- ✅ Перезапустит PM2 процессы
- ✅ Сохранит конфигурацию PM2
- ✅ Покажет статус

---

## 🔧 ВАРИАНТ 2: Использовать DEPLOY.sh (Автоматический)

Если у вас настроен SSH доступ к серверу:

### **Шаг 1: Настроить переменные окружения**

На **локальной машине**, перед запуском DEPLOY.sh:

```bash
# Экспортировать переменные (на macOS/Linux)
export SERVER_USER="root"
export SERVER_HOST="your-server-ip"  # или домен
export SERVER_PATH="/opt/lensroom/current"
export PM2_APP_NAME="lensroom"

# Проверить
echo "Деплой на: $SERVER_USER@$SERVER_HOST:$SERVER_PATH"
```

### **Шаг 2: Запустить деплой**

```bash
cd /Users/maratsagimov/Desktop/LensRoom.V2/lensroom-v2

# Запустить автоматический деплой
bash DEPLOY.sh
```

**Скрипт выполнит:**
1. ✅ Проверит git статус (должно быть чисто)
2. ✅ Проверит текущую ветку
3. ✅ Запустит тестовый build
4. ✅ Push на remote (опционально)
5. ✅ Подключится к серверу через SSH
6. ✅ Git pull на сервере
7. ✅ Установит зависимости (npm ci)
8. ✅ Соберёт production build
9. ✅ Перезапустит PM2
10. ✅ Проверит статус

---

## 🔧 ВАРИАНТ 3: Ручной rsync (если нет Git на сервере)

### **Шаг 1: Синхронизировать файлы**

```bash
cd /Users/maratsagimov/Desktop/LensRoom.V2/lensroom-v2

# Rsync с исключением node_modules и .next
rsync -avz --progress \
  --exclude 'node_modules' \
  --exclude '.next' \
  --exclude '.git' \
  --exclude '.env.local' \
  ./ root@your-server-ip:/opt/lensroom/current/
```

### **Шаг 2: На сервере - деплой**

```bash
ssh root@your-server-ip

cd /opt/lensroom/current

# Запустить деплой
bash scripts/deploy-production.sh
```

---

## 📝 ЧТО ДЕЛАТЬ ПРЯМО СЕЙЧАС:

### **Быстрый старт (рекомендую):**

```bash
# 1. На локальной машине - экспортировать переменные
export SERVER_USER="root"
export SERVER_HOST="ваш-ip-сервера"
export SERVER_PATH="/opt/lensroom/current"
export PM2_APP_NAME="lensroom"

# 2. Запустить автоматический деплой
cd /Users/maratsagimov/Desktop/LensRoom.V2/lensroom-v2
bash DEPLOY.sh
```

**Или если скрипт просит Git push и вы не хотите:**
- Нажмите `n` когда спросит "Push to origin/main?"
- Нажмите `y` когда спросит "Deploy to server?"

---

## 🔍 ПРОВЕРКА ПОСЛЕ ДЕПЛОЯ:

### **1. Проверить PM2 статус:**

```bash
ssh root@your-server-ip
pm2 status
```

Должно быть:
```
┌─────┬──────────────────┬─────────┬─────────┬─────────┬──────────┐
│ id  │ name             │ status  │ restart │ uptime  │ cpu      │
├─────┼──────────────────┼─────────┼─────────┼─────────┼──────────┤
│ 0   │ lensroom         │ online  │ 0       │ 10s     │ 0%       │
│ 1   │ lensroom-previews│ online  │ 0       │ 10s     │ 0%       │
└─────┴──────────────────┴─────────┴─────────┴─────────┴──────────┘
```

### **2. Проверить логи:**

```bash
# Основное приложение
pm2 logs lensroom --lines 50

# Preview worker
pm2 logs lensroom-previews-worker --lines 50

# Все логи вместе
pm2 logs --lines 100
```

### **3. Проверить сайт:**

Откройте в браузере:
```
http://ваш-домен/create
```

**Проверьте новые функции:**
- [ ] Кнопка "Remix" в хедере
- [ ] Кнопка "Batch" в хедере
- [ ] Загрузка изображений работает
- [ ] BatchProgressBar появляется
- [ ] Кнопка "Remix" в истории

---

## 🐛 ЕСЛИ ЧТО-ТО ПОШЛО НЕ ТАК:

### **Откатить на предыдущую версию:**

```bash
ssh root@your-server-ip
cd /opt/lensroom/current

# Откатить git на 1 коммит назад
git reset --hard HEAD~1

# Пересобрать
npm ci
npm run build

# Перезапустить
pm2 restart all --update-env
```

### **Проверить ошибки сборки:**

```bash
ssh root@your-server-ip
cd /opt/lensroom/current

# Попробовать собрать вручную
npm run build

# Смотреть ошибки
```

### **Очистить кэш:**

```bash
ssh root@your-server-ip
cd /opt/lensroom/current

# Удалить .next и node_modules
rm -rf .next node_modules

# Переустановить и пересобрать
npm ci
npm run build
pm2 restart all --update-env
```

---

## 📊 МОНИТОРИНГ ПОСЛЕ ДЕПЛОЯ:

### **PM2 команды:**

```bash
# Статус всех процессов
pm2 status

# Логи в реальном времени
pm2 logs

# Только ошибки
pm2 logs --err

# Монит (CPU/Memory)
pm2 monit

# Информация о процессе
pm2 describe lensroom

# Перезапустить с новым env
pm2 restart all --update-env

# Сохранить конфигурацию
pm2 save
```

### **Полезные команды:**

```bash
# Проверить порт 3002
netstat -tuln | grep 3002

# Curl test
curl http://localhost:3002

# Проверить Nginx (если используется)
nginx -t
systemctl status nginx
```

---

## ✅ ЧЕКЛИСТ ДЕПЛОЯ:

- [ ] **Локально:** Commit создан (af6d64c)
- [ ] **Сервер:** SSH доступ есть
- [ ] **Сервер:** Git установлен
- [ ] **Сервер:** Node.js установлен
- [ ] **Сервер:** PM2 установлен
- [ ] **Деплой:** Скрипт запущен
- [ ] **Проверка:** PM2 статус online
- [ ] **Проверка:** Логи без ошибок
- [ ] **Проверка:** Сайт открывается
- [ ] **Проверка:** Новые функции работают

---

## 🎯 РЕКОМЕНДАЦИЯ:

**Используйте DEPLOY.sh - это самый простой способ!**

```bash
# Просто запустите:
bash DEPLOY.sh
```

Скрипт всё сделает автоматически и покажет каждый шаг.

---

## 📞 НУЖНА ПОМОЩЬ?

Если возникли проблемы, предоставьте:
1. Output команды `git remote -v`
2. Output команды `pm2 status` (на сервере)
3. Логи: `pm2 logs --lines 100 --err`
4. IP или домен вашего сервера

---

**🚀 Готово к деплою!**

