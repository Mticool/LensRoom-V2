# Обновление: Управление чатами/сессиями генерации

## Внесенные изменения

### 1. **Уменьшены отступы в галерее истории** (HistorySidebar.tsx)
- `space-y-1.5` → `space-y-1` (отступы между элементами)
- `p-2` → `p-1.5` (внутренние отступы элементов)
- `px-2 pb-2` → `px-1.5 pb-1.5` (отступы для секции действий)

**Результат:** Галерея стала более компактной, меньше пустого пространства между 4 фотографиями.

---

### 2. **Добавлена кнопка "Новый чат"** (GeneratorV2.tsx)
- Новая кнопка с иконкой `Plus` в верхней панели
- Расположена рядом с переключателем режимов (Фото/Видео)
- Функция `handleNewChat()` очищает:
  - Текущий результат (`currentResult`)
  - Промпт (`currentPrompt`)
  - Референсные изображения/видео
  - Автоматически скрывает историю

**Визуальные индикаторы кнопки:**
- **Новый чат активен:** подсвечена синим (`bg-[#00D9FF]/10 border border-[#00D9FF]/30`) с пульсирующей точкой
- **Продолжение чата:** серый фон (`bg-[#27272A]`), при наведении меняется на синий

---

### 3. **Изменено поведение по умолчанию**
#### До изменений:
- При открытии генератора история автоматически открыта на desktop
- Пользователь видел галерею, а не чистый холст
- При смене модели/режима история оставалась открытой

#### После изменений:
- История **скрыта по умолчанию** при загрузке
- Пользователь начинает с **нового чата** (чистый холст)
- История **автоматически скрывается** при:
  - Переключении режима (Фото ↔ Видео)
  - Выборе новой модели (нейросети) в настройках
  - Клике на кнопку "Новый чат"
- История автоматически открывается только когда:
  - Пользователь выбирает элемент из истории (продолжает работу в чате)
  - Пользователь кликает на кнопку "История"

---

### 4. **Визуальные индикаторы состояния чата** (Canvas.tsx)

#### Новый чат:
```jsx
<div className="inline-flex items-center gap-2 px-3 py-1.5 rounded-full bg-[#00D9FF]/10 border border-[#00D9FF]/20">
  <div className="w-2 h-2 rounded-full bg-[#00D9FF] animate-pulse" />
  <span className="text-xs font-medium text-[#00D9FF]">Новый чат</span>
</div>
```
- Отображается в пустом состоянии Canvas
- Синий цвет с пульсирующей точкой
- Показывает, что пользователь работает в новой сессии

#### Продолжение чата:
```jsx
<div className="inline-flex items-center gap-2 px-3 py-1.5 rounded-full bg-[#27272A] border border-[#3F3F46]">
  <div className="w-2 h-2 rounded-full bg-emerald-400" />
  <span className="text-xs font-medium text-[#A1A1AA]">Продолжение чата</span>
</div>
```
- Отображается когда есть текущий результат
- Серый фон с зеленой точкой
- Показывает, что пользователь продолжает работу в существующем чате

---

## Пользовательский сценарий

### Сценарий 1: Новый пользователь открывает генератор
1. Видит чистый холст с индикатором "Новый чат" 🟦
2. История скрыта
3. Кнопка "Новый" подсвечена синим с пульсирующей точкой
4. Создает первую генерацию

### Сценарий 2: Продолжение работы
1. Выбирает элемент из истории (клик на элемент в истории)
2. История автоматически открывается (на desktop)
3. Индикатор меняется на "Продолжение чата" 🟢
4. Кнопка "Новый" становится серой (не активна)
5. Может создать новую генерацию в контексте текущего чата

### Сценарий 3: Начать новый чат
1. Кликает на кнопку "Новый"
2. История автоматически скрывается
3. Промпт и результаты очищаются
4. Индикатор меняется на "Новый чат" 🟦
5. Может начать работу с чистого листа

---

## Технические детали

### Измененные файлы:
1. **`src/components/generator-v2/GeneratorV2.tsx`**
   - Импорт иконки `Plus`
   - Функция `handleNewChat()`
   - Логика `handleSelectFromHistory()` с auto-show history
   - Изменено поведение `useEffect` для `checkMobile`
   - **NEW:** useEffect для скрытия истории при смене режима (mode)
   - **NEW:** useEffect для скрытия истории при смене модели (settings.model)
   - Визуальные индикаторы кнопки "Новый"

2. **`src/components/generator-v2/HistorySidebar.tsx`**
   - Уменьшены отступы для компактности

3. **`src/components/generator-v2/Canvas.tsx`**
   - Добавлены индикаторы "Новый чат" и "Продолжение чата"

### Зависимости:
- `lucide-react` (иконка `Plus`)
- `sonner` (toast уведомления)

---

## Результат

✅ Пользователь видит четкое разделение между "новым чатом" и "продолжением чата"
✅ История не отвлекает при начале новой работы
✅ Галерея стала более компактной
✅ Интуитивно понятный UX для управления сессиями генерации
