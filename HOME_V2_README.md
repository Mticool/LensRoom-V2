# Home V2 - Netflix-Style Content Showcase

Новая главная страница LensRoom в стиле контентной витрины с акцентом на "New & Trending" и готовые сценарии использования.

## 🎯 Что реализовано

### ✅ Компоненты

1. **HeroNew** (`src/components/home-v2/HeroNew.tsx`)
   - Короткий сильный оффер
   - Главный CTA "Создать сейчас"
   - Вторичный CTA "Смотреть тренды"
   - Trust indicators (3 преимущества)

2. **TrendingGrid** (`src/components/home-v2/TrendingGrid.tsx`)
   - Адаптивная сетка: 4/2/1 колонки (desktop/tablet/mobile)
   - Карточки с превью (только `<img>`, БЕЗ `<video>` для производительности)
   - Бейджи: модель, тип (фото/видео), длительность
   - Интерактивные кнопки на hover:
     - **Recreate** (главная) - воссоздать точную копию
     - **Remix** (вторичная) - создать вариацию
   - Загрузка данных из API с фоллбэком на демо-пресеты

3. **TopChoice** (`src/components/home-v2/TopChoice.tsx`)
   - Кураторская подборка "Лучшие инструменты недели"
   - 4 карточки с TOP бейджем
   - Быстрый доступ к созданию

4. **AppsSection** (`src/components/home-v2/AppsSection.tsx`)
   - 6 готовых сценариев использования:
     - UGC Content
     - Advertising
     - E-Commerce
     - AI Avatars
     - Image Enhancement
     - Social Media
   - Каждый с иконкой, описанием и тегами

5. **HowItWorks** (`src/components/home-v2/HowItWorks.tsx`)
   - 3 простых шага
   - Визуальная последовательность с иконками

6. **PricingSection** (`src/components/home-v2/PricingSection.tsx`)
   - 4 тарифа: Free, Creator, Pro, Studio
   - Промо-баннер
   - CTA "Купить ⭐ отдельно"

7. **FAQ** (`src/components/home-v2/FAQ.tsx`)
   - 8 частых вопросов с аккордеоном
   - Охват основных сценариев

8. **FooterCTA** (`src/components/home-v2/FooterCTA.tsx`)
   - Финальный призыв к действию
   - Два CTA: "Начать создавать" + "Смотреть тарифы"

### ✅ Логика и данные

1. **homePresets.ts** (`src/lib/homePresets.ts`)
   - Типы: `HomeItem`, `AppPack`, `HomePresetParams`
   - Демо-данные: `TRENDING_PRESETS`, `TOP_CHOICE_PRESETS`, `APP_PACKS`
   - Helper: `itemToDraft()` для конвертации в draft

2. **draft-preset.ts** (`src/lib/draft-preset.ts`)
   - Работа с localStorage (ключ: `lensroom:presetDraft`)
   - Функции:
     - `saveDraftPreset()` - сохранить draft
     - `loadDraftPreset()` - загрузить draft
     - `clearDraftPreset()` - очистить draft
     - `handleRecreate()` - Recreate flow (save + redirect)
     - `handleRemix()` - Remix flow (save + redirect)
   - TTL: 24 часа

3. **API endpoint** (`src/app/api/home/trending/route.ts`)
   - `GET /api/home/trending`
   - Логика:
     - Если пользователь авторизован → его последние 12 успешных генераций
     - Если не авторизован или нет данных → демо-пресеты
   - Возвращает `{ items: HomeItem[], source: 'user' | 'demo' }`

### ✅ Recreate/Remix Flow

**Как работает:**

1. Пользователь кликает на "Recreate" или "Remix" в карточке
2. Создаётся объект `DraftPreset` с параметрами генерации
3. Draft сохраняется в `localStorage` под ключом `lensroom:presetDraft`
4. Редирект на `/create/studio`
5. На странице studio: проверить наличие draft → если есть → предзаполнить форму

**Структура DraftPreset:**
```typescript
{
  model: string,              // 'flux-pro', 'kling-1.5', etc.
  modelDisplayName?: string,
  type: 'photo' | 'video',
  params: {
    aspect?: AspectRatio,
    resolution?: string,
    durationSec?: number,
    mode?: 'fast' | 'quality',
    // ...
  },
  prompt: string,
  sourceItem?: string,        // ID исходной карточки
  mode?: 'recreate' | 'remix',
  timestamp?: number          // для TTL
}
```

## 🚀 Включение Home V2

### 1. Environment Variable

Добавьте в `.env.local`:

```bash
NEXT_PUBLIC_HOME_V2=1
```

### 2. Перезапуск dev сервера

```bash
npm run dev
```

### 3. Проверка

Откройте `http://localhost:3000` - должна загрузиться новая главная страница.

## 📁 Структура файлов

```
src/
├── lib/
│   ├── homePresets.ts          # Типы и демо-данные
│   └── draft-preset.ts         # localStorage flow для Recreate/Remix
│
├── app/
│   ├── page.tsx                # Main page с feature flag
│   └── api/
│       └── home/
│           └── trending/
│               └── route.ts    # API endpoint для trending данных
│
└── components/
    └── home-v2/
        ├── HomeV2.tsx          # Главный компонент
        ├── HeroNew.tsx         # Hero секция
        ├── TrendingGrid.tsx    # New & Trending сетка
        ├── TopChoice.tsx       # Кураторская подборка
        ├── AppsSection.tsx     # Use cases / Apps
        ├── HowItWorks.tsx      # Как работает (3 шага)
        ├── PricingSection.tsx  # Тарифы
        ├── FAQ.tsx             # Частые вопросы
        └── FooterCTA.tsx       # Финальный CTA
```

## 🔌 Интеграция с Studio

Для интеграции draft flow в `/create/studio`, добавьте в studio page:

```typescript
import { loadDraftPreset, clearDraftPreset } from '@/lib/draft-preset';

// В компоненте или useEffect:
useEffect(() => {
  const draft = loadDraftPreset();
  
  if (draft) {
    // Предзаполнить форму
    setModel(draft.model);
    setType(draft.type);
    setPrompt(draft.prompt);
    setParams(draft.params);
    
    // Опционально: показать уведомление
    toast.success(`Loaded ${draft.mode} preset`);
    
    // Очистить draft после использования
    clearDraftPreset();
  }
}, []);
```

## 📊 Данные и фоллбэки

### Режим 1: Пользователь авторизован
- API `/api/home/trending` возвращает последние успешные генерации пользователя
- Автоматическая конвертация в формат `HomeItem`
- Signed URLs с TTL 60 минут

### Режим 2: Демо-режим
- Используются `TRENDING_PRESETS` из `homePresets.ts`
- Плейсхолдеры через `picsum.photos` (стабильные ID для консистентности)
- Можно заменить на реальные демо-изображения в `/public/home-demo/`

## 🎨 Стиль и UX

### Дизайн-система
- **Цвета**: CSS переменные (`--bg`, `--text`, `--gold`, `--border`, etc.)
- **Компоненты**: Shadcn UI (`Button`, `Badge`, etc.)
- **Стили**: Tailwind CSS
- **Тема**: Premium/luxury, референсы: Stripe, Linear, Vercel

### Адаптивность
- Mobile-first подход
- Breakpoints:
  - `mobile`: 1 колонка
  - `md` (768px+): 2 колонки
  - `lg` (1024px+): 4 колонки

### Производительность
- ✅ Только `<img>` в сетке (БЕЗ `<video>`)
- ✅ `next/image` для оптимизации
- ✅ Lazy loading
- ✅ Skeleton loaders при загрузке

## ✅ Acceptance Criteria

- [x] Главная визуально "дорогая", читаемая, без перегруза
- [x] "New & Trending" есть и выглядит как витрина
- [x] Карточки НЕ используют `<video>` в сетке
- [x] Recreate/Remix не "мёртвые": сохраняют draft и ведут на `/create/studio`
- [x] Если нет данных — показываются демо элементы (без пустоты)
- [x] Ниже есть блок Pricing и FAQ
- [x] Проект собирается (`npm run build`) без ошибок

## 🐛 Troubleshooting

### Проблема: Home V2 не включается
**Решение**: Проверьте `.env.local` на наличие `NEXT_PUBLIC_HOME_V2=1` и перезапустите dev сервер.

### Проблема: Демо-изображения не загружаются
**Решение**: Замените `picsum.photos` URL в `homePresets.ts` на локальные изображения в `/public/home-demo/`.

### Проблема: Draft не подхватывается в Studio
**Решение**: Добавьте логику загрузки draft в studio page (см. секцию "Интеграция с Studio").

### Проблема: TypeScript ошибки
**Решение**: Проверьте импорты и типы. Все типы определены в `homePresets.ts`.

## 📝 Дальнейшие улучшения

1. **Реальные превью**: Заменить `picsum.photos` на реальные демо-изображения
2. **Анимации**: Добавить framer-motion для плавных переходов
3. **Персонализация**: Показывать разные trending для разных пользователей
4. **A/B тесты**: Тестировать разные варианты hero текста
5. **Analytics**: Отслеживать клики на Recreate/Remix
6. **Download**: Добавить функцию скачивания в карточках

## 🎉 Готово!

Новая главная страница готова к использованию. Включите feature flag и наслаждайтесь премиальным UX! 🚀

