# 📊 Отслеживание лимитов Free тарифа

## ✅ Что реализовано

Система отслеживания лимитов для бесплатного тарифа и автоматического предложения апгрейда.

### 1. SQL функции (`030_usage_limits.sql`)
- ✅ `get_daily_usage()` — подсчет генераций за сегодня
- ✅ `check_user_limits()` — проверка лимитов и определение нужен ли upsell
- ✅ Индекс для быстрого подсчета

### 2. API endpoint (`/api/limits/check`)
- ✅ GET запрос для проверки лимитов
- ✅ Возвращает использование и доступные лимиты
- ✅ Определяет нужен ли upsell

### 3. React компоненты
- ✅ `LimitReachedModal` — модалка с предложением апгрейда
- ✅ `useLimitsCheck()` — хук для проверки лимитов

---

## 📝 Как это работает

### Лимиты Free тарифа:
- **Nano Banana:** 5 генераций в день
- **Nano Pro:** 0 (недоступно)
- **Инструменты:** 5 использований в день

### Логика:
1. Пользователь делает генерацию
2. API проверяет лимиты через `/api/limits/check`
3. Если лимит достигнут → показывается модалка с предложением
4. Модалка рекомендует тарифы **Lite** или **Creator**

---

## 🚀 Интеграция

### Шаг 1: Примените миграцию

```bash
# В Supabase SQL Editor выполните
supabase/migrations/030_usage_limits.sql
```

### Шаг 2: Добавьте в генератор

**Пример для `src/app/create/studio/page.tsx`:**

```typescript
'use client';

import { useState, useEffect } from 'react';
import { LimitReachedModal, useLimitsCheck } from '@/components/limits/LimitReachedModal';

export default function StudioPage() {
  const { limits, shouldShowModal, checkLimits, closeModal } = useLimitsCheck();
  const [generating, setGenerating] = useState(false);

  // Проверить лимиты при загрузке страницы
  useEffect(() => {
    checkLimits();
  }, []);

  const handleGenerate = async () => {
    // 1. Проверить лимиты ПЕРЕД генерацией
    await checkLimits();
    
    if (limits?.shouldUpsell) {
      // Лимит достигнут - модалка покажется автоматически
      return;
    }

    // 2. Продолжить генерацию
    setGenerating(true);
    try {
      const res = await fetch('/api/generate/photo', {
        method: 'POST',
        body: JSON.stringify({ /* ... */ })
      });
      
      // 3. После генерации снова проверить (может достигнут лимит)
      await checkLimits();
    } finally {
      setGenerating(false);
    }
  };

  return (
    <div>
      {/* Ваш UI генератора */}
      <button onClick={handleGenerate} disabled={generating}>
        Сгенерировать
      </button>

      {/* Модалка лимита */}
      <LimitReachedModal
        isOpen={shouldShowModal}
        onClose={closeModal}
        reason={limits?.upsellReason}
        usage={{
          nanoBanana: limits?.usage.nanoBanana,
          tools: limits?.usage.tools
        }}
      />
    </div>
  );
}
```

### Шаг 3: Альтернативный вариант - проверка в API

**В `src/app/api/generate/photo/route.ts` ПЕРЕД генерацией:**

```typescript
// Проверить лимиты перед генерацией
const { data: limitsData } = await supabase.rpc('check_user_limits', {
  user_uuid: userId
});

if (limitsData?.[0]?.should_upsell) {
  return NextResponse.json(
    {
      error: 'Limit reached',
      shouldUpsell: true,
      reason: limitsData[0].upsell_reason,
      limits: limitsData[0]
    },
    { status: 402 } // Payment Required
  );
}

// Продолжить генерацию...
```

---

## 📊 API Response пример

**GET `/api/limits/check`**

```json
{
  "hasSubscription": false,
  "subscriptionTier": "free",
  "usage": {
    "nanoBanana": {
      "used": 5,
      "limit": 5,
      "available": false,
      "remaining": 0
    },
    "nanoPro": {
      "used": 0,
      "limit": 0,
      "available": false,
      "remaining": 0
    },
    "tools": {
      "used": 3,
      "limit": 5,
      "available": true,
      "remaining": 2
    }
  },
  "shouldUpsell": true,
  "upsellReason": "Достигнут дневной лимит Nano Banana (5/день)",
  "upsellCTA": {
    "title": "🚀 Достигнут лимит бесплатного тарифа",
    "description": "Достигнут дневной лимит Nano Banana (5/день)",
    "action": "Выбрать тариф",
    "href": "/pricing"
  }
}
```

---

## 🎨 UI компонент

### LimitReachedModal
Красивая модалка с:
- ✅ Информацией о достигнутом лимите
- ✅ Текущим использованием
- ✅ Рекомендуемыми тарифами (Lite, Creator)
- ✅ CTA кнопками "Выбрать тариф" / "Вернуться завтра"
- ✅ Анимациями (framer-motion)

### useLimitsCheck Hook
Удобный хук для:
- Проверки лимитов
- Автопоказа модалки
- Управления состоянием

---

## 🔄 Обновление лимитов

Лимиты Free тарифа **обновляются автоматически каждый день в 00:00** (проверка через SQL `created_at >= CURRENT_DATE`).

Никакого cron job не нужно!

---

## 📈 Метрики для отслеживания

Рекомендуем отслеживать:
1. **Conversion rate:** Сколько пользователей free перешло на платный тариф после показа модалки
2. **Достижение лимита:** Сколько пользователей достигло лимита 5/день
3. **Средние генерации на free:** Сколько генераций делают free пользователи

SQL для аналитики:
```sql
-- Пользователи которые достигли лимита сегодня
SELECT COUNT(DISTINCT user_id) 
FROM generations
WHERE created_at >= CURRENT_DATE
GROUP BY user_id
HAVING COUNT(*) >= 5;
```

---

## ✅ Тестирование

### Локальное тестирование:

1. **Создайте 5 генераций:**
   ```bash
   # В UI создайте 5 генераций на Free тарифе
   ```

2. **Проверьте API:**
   ```bash
   curl http://localhost:3000/api/limits/check \
     -H "Cookie: your-session-cookie"
   ```

3. **Попробуйте 6-ю генерацию:**
   - Должна показаться модалка

---

## 🎯 Результат

✅ **Пользователи Free тарифа:**
- Видят лимит 5 генераций/день
- Получают предложение апгрейда при достижении лимита
- Могут выбрать тариф прямо из модалки

✅ **Для бизнеса:**
- Автоматический upsell механизм
- Красивая модалка с конверсией
- Метрики для оптимизации

---

## 📁 Созданные файлы

1. ✅ `supabase/migrations/030_usage_limits.sql` — SQL функции
2. ✅ `src/app/api/limits/check/route.ts` — API endpoint
3. ✅ `src/components/limits/LimitReachedModal.tsx` — React компонент
4. ✅ `LIMITS_TRACKING_GUIDE.md` — эта документация

---

## 🚀 Готово к использованию!

1. Примените миграцию в Supabase
2. Интегрируйте компонент в генератор
3. Тестируйте и оптимизируйте conversion

**Вопросы? Смотрите примеры интеграции выше!** 💪

